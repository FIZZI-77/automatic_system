apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - elasticsearch.yaml
  - kibana.yaml
  - filebeat.yaml

configMapGenerator:
  - name: filebeat-config
    files:
      - filebeat.yml=filebeat-config.yaml

generatorOptions:
  disableNameSuffixHash: true
