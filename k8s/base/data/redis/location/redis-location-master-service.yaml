apiVersion: v1
kind: Service
metadata:
  name: redis-location-master
  namespace: automatic-system
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/component: location-master
spec:
  selector:
    app: redis-location-master
  ports:
    - name: redis
      port: 6379
      targetPort: 6379