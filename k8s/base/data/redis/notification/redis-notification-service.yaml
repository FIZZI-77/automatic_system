apiVersion: v1
kind: Service
metadata:
  name: redis-notification
  namespace: automatic-system
spec:
  selector:
    app: redis-notification
  ports:
    - name: redis
      port: 6379
      targetPort: 6379