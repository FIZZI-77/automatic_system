apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: automatic-system
spec:
  selector:
    app: frontend
  ports:
    - name: http
      appProtocol: http
      port: 3000
      targetPort: http
