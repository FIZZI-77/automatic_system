# Asset Service

## Общее описание и общий принцип работы

`Asset_Service` ведет реестр объектов городской инфраструктуры: карточки и
геометрию, историю состояния, аварии, ремонты, осмотры и планы обслуживания.
PostGIS используется для пространственного поиска. Изменения публикуются через
транзакционную таблицу `outbox_events`.

После аварии, ремонта или осмотра сервис пересчитывает риск отказа. Это
объяснимая балльная формула, а не обученная модель. Итог ограничивается
диапазоном 0–100, преобразуется в вероятность на 90 дней и сохраняется вместе с
причинами и рекомендуемым действием.

Изменяющие операции принимают `p bool`. Значение `false` возвращает
`ErrForbidden`; значение формируется транспортным слоем из прав пользователя.

## Модели

### Перечисления

| Тип | Значения | Назначение |
|---|---|---|
| `Status` | `PLANNED`, `ACTIVE`, `DAMAGED`, `UNDER_REPAIR`, `NEEDS_REPLACEMENT`, `DECOMMISSIONED` | Состояние объекта. |
| `RiskLevel` | `LOW`, `MEDIUM`, `HIGH`, `CRITICAL` | Уровень рассчитанного риска. |

### `Asset`

| Поле | Тип Go | Назначение |
|---|---|---|
| `ID` | `uuid.UUID` | Идентификатор объекта. |
| `ExternalID` | `*string` | Необязательный идентификатор внешней системы. |
| `DepartmentID` | `uuid.UUID` | Ответственное подразделение. |
| `Type` | `string` | Тип объекта. |
| `Name` | `string` | Название. |
| `Address` | `string` | Адрес. |
| `District` | `string` | Район. |
| `Municipality` | `string` | Муниципальное образование. |
| `Geometry` | `string` | Геометрия в представлении, принятом репозиторием PostGIS. |
| `Model` | `string` | Модель оборудования. |
| `SerialNumber` | `string` | Серийный номер. |
| `Owner` | `string` | Владелец. |
| `ServiceOrganization` | `string` | Обслуживающая организация. |
| `Contractor` | `string` | Подрядчик. |
| `Status` | `Status` | Текущее состояние. |
| `InstallationYear` | `*int32` | Год установки. |
| `ServiceLifeYears` | `*int32` | Нормативный срок службы в годах. |
| `WarrantyUntil` | `*time.Time` | Окончание гарантии. |
| `InspectionIntervalDays` | `int32` | Интервал осмотра в днях. |
| `ResponseNormMinutes` | `int32` | Норматив реакции в минутах. |
| `RepairNormMinutes` | `int32` | Норматив ремонта в минутах. |
| `Criticality` | `float64` | Критичность от 0 до 1. |
| `RiskScore` | `float64` | Последняя оценка риска от 0 до 100. |
| `RiskLevel` | `RiskLevel` | Последний уровень риска. |
| `LastRepairAt` | `*time.Time` | Время последнего ремонта. |
| `NextInspectionAt` | `*time.Time` | Плановая дата следующего осмотра. |
| `CreatedAt`, `UpdatedAt` | `time.Time` | Время создания и изменения. |

### Остальные структуры

| Структура | Поля и назначение |
|---|---|
| `CreateInput` | Встраивает все поля `Asset`; `ActorID` — автор создания. |
| `UpdateInput` | `ID` — объект; необязательные `Name`, `Address`, `Geometry`, `Contractor`, `Criticality` задают только изменяемые значения. |
| `Filter` | `DepartmentID` — подразделение; `Type` — тип; `District` — район; `Status` — состояние; `RiskLevel` — риск; `Limit`, `Offset` — страница. |
| `Prediction` | `AssetID` — объект; `Score` — балл; `Probability` — расчетная вероятность в процентах; `Level` — уровень; `Factors` — объясняющие причины; `Action` — рекомендация; `CalculatedAt` — время расчета. |
| `Incident` | `ID` — авария; `AssetID` — объект; `TicketID` — необязательная заявка; `FailureType` — вид отказа; `Description` — описание; `Source` — источник; `Priority` — приоритет; `Repeated` — повторность; `OccurredAt` — время. |
| `Repair` | `ID` — ремонт; `AssetID` — объект; `IncidentID`, `TicketID`, `BrigadeID` — необязательные связи; `Description` — работы; `ReplacedComponents` — замененные части; `DurationMinutes` — длительность; `CompletedAt` — завершение. |
| `Inspection` | `ID` — осмотр; `AssetID` — объект; `InspectorID` — проверяющий; `Kind` — вид; `Result` — результат; `Recommendation` — рекомендация; `DefectFound` — найден дефект; `ConditionScore` — состояние от 0 до 1; `InspectedAt` — время. |
| `Plan` | `ID` — план; `AssetID` — объект; `Kind` — вид обслуживания; `IntervalDays` — период; `NextDueAt` — следующий срок; `Active` — активность; `LastCompletedAt` — последнее выполнение. |

## Функции

### `NewService`

Создает оболочку `Service` и реализацию с репозиторием и журналом.

### `Create`

Требует право изменения, ненулевое подразделение, название, тип и геометрию.
Критичность должна быть от 0 до 1. После проверки вызывает репозиторий и
записывает созданный идентификатор и тип в журнал.

### `Get`

Возвращает объект по UUID напрямую из репозитория.

### `Update`

Требует право изменения. Если критичность передана, проверяет диапазон 0–1,
после чего выполняет частичное обновление и журналирует успех.

### `List`

Нормализует `Limit`: неположительное значение заменяет на 20, значение больше
100 — на 100. Передает фильтр и смещение репозиторию.

### `ChangeStatus`

Требует право изменения и передает объект, новое состояние, автора и причину
репозиторию. Репозиторий изменяет карточку и пишет историю состояния.

### `Nearby`

Проверяет широту от -90 до 90, долготу от -180 до 180 и положительный радиус.
Предел вне диапазона 1–100 заменяет на 20. Передает координаты, радиус,
необязательный тип и предел пространственному запросу репозитория.

### `Incident`

Требует право изменения, записывает аварию через `RecordIncident`, затем
немедленно вызывает `calculate` для объекта. Возвращает и аварию, и новый
прогноз; ошибка расчета возникает уже после успешной записи аварии.

### `Repair`

Требует право, завершает ремонт и затем пересчитывает риск. Как и для аварии,
ошибка расчета не отменяет уже сохраненный ремонт.

### `Inspection`

Требует право и оценку состояния от 0 до 1. Записывает осмотр и пересчитывает
риск объекта.

### `CreatePlan`

Требует право и положительный `IntervalDays`, затем создает план через
репозиторий.

### `Due`

Заменяет неположительный предел на 20. Возвращает планы, срок которых наступил
к моменту `t`, с необязательным ограничением по подразделению и смещением.
Верхняя граница предела в прикладном слое не задается.

### `Prediction`

Пытается прочитать последний прогноз. Только если репозиторий сообщает «не
найдено», рассчитывает и сохраняет новый прогноз на текущее время UTC.
Остальные ошибки возвращает без пересчета.

### `Recalculate`

Требует право, получает идентификаторы объектов выбранного подразделения или
всех объектов и последовательно вызывает `calculate`. При первой ошибке
останавливается и возвращает число уже пересчитанных объектов.

### `calculate`

1. Загружает факты риска на момент `now`.
2. Начинает с `Criticality * 20`.
3. При известном положительном сроке службы добавляет не более 25 баллов:
   `age / serviceLife * 25`. При доле не меньше 0,8 добавляет причину об
   исчерпании срока.
4. За события 90 дней добавляет не более 25 баллов:
   `Incidents90 * 7 + Repeat90 * 5`. Повторы добавляют отдельную причину.
5. За просрочку осмотра добавляет не более 15 баллов:
   `DaysInspectionOverdue / 10`.
6. При известной последней оценке состояния добавляет
   `(1 - LastCondition) * 15`; значение ниже 0,5 отмечается как плохое.
7. Ограничивает сумму 100 и округляет до одной десятой.
8. Назначает уровень: `MEDIUM` от 35, `HIGH` от 60, `CRITICAL` от 75,
   иначе `LOW`; одновременно выбирает текст рекомендации.
9. Вычисляет вероятность:
   `round((1 - exp(-score / 55)) * 1000) / 10`.
10. Сохраняет прогноз и возвращает его.

### `logger`

Возвращает настроенный `zap.Logger`, а при его отсутствии — `zap.NewNop()`.

## Структура БД

### `assets` — карточки объектов

| Поле | Тип PostgreSQL | Назначение |
|---|---|---|
| `id` | `uuid` | Первичный ключ. |
| `external_id` | `text` | Уникальный внешний идентификатор. |
| `department_id` | `uuid` | Ответственное подразделение. |
| `type`, `name` | `text` | Тип и название. |
| `address`, `district`, `municipality` | `text` | Адрес, район и муниципалитет. |
| `geometry` | `geometry(Geometry,4326)` | Геометрия PostGIS в WGS 84. |
| `status` | `text` | Состояние, по умолчанию `ACTIVE`. |
| `model`, `serial_number` | `text` | Модель и серийный номер. |
| `installation_year`, `service_life_years` | `int` | Год установки и срок службы. |
| `warranty_until` | `timestamptz` | Окончание гарантии. |
| `owner`, `service_organization`, `contractor` | `text` | Владелец, обслуживающая организация и подрядчик. |
| `inspection_interval_days` | `int` | Интервал осмотра, по умолчанию 365. |
| `response_norm_minutes` | `int` | Норматив реакции, по умолчанию 60. |
| `repair_norm_minutes` | `int` | Норматив ремонта, по умолчанию 1440. |
| `criticality` | `double precision` | Критичность, по умолчанию 0,5. |
| `risk_score` | `double precision` | Последний балл риска. |
| `risk_level` | `text` | Последний уровень риска. |
| `last_repair_at`, `next_inspection_at` | `timestamptz` | Последний ремонт и следующий осмотр. |
| `created_at`, `updated_at` | `timestamptz` | Времена записи. |

Пространственный индекс создан по `geometry`, составной индекс — по
`department_id`, `type`, `status`, `risk_level`.

### `asset_status_history` — история состояний

| Поле | Тип | Назначение |
|---|---|---|
| `id` | `uuid` | Первичный ключ. |
| `asset_id` | `uuid` | Объект, внешний ключ на `assets`. |
| `old_status`, `new_status` | `text` | Предыдущее и новое состояния. |
| `reason` | `text` | Причина изменения. |
| `changed_by` | `uuid` | Автор изменения. |
| `created_at` | `timestamptz` | Время изменения. |

### `asset_incidents` — аварии

| Поле | Тип | Назначение |
|---|---|---|
| `id`, `asset_id` | `uuid` | Авария и объект. |
| `ticket_id` | `uuid` | Связанная заявка. |
| `failure_type`, `description`, `source`, `priority` | `text` | Вид отказа, описание, источник и приоритет. |
| `repeated` | `bool` | Повторный случай. |
| `occurred_at` | `timestamptz` | Время аварии. |

### `asset_repairs` — ремонты

| Поле | Тип | Назначение |
|---|---|---|
| `id`, `asset_id` | `uuid` | Ремонт и объект. |
| `incident_id`, `ticket_id`, `brigade_id` | `uuid` | Необязательные связи с аварией, заявкой и бригадой. |
| `description`, `replaced_components` | `text` | Работы и замененные части. |
| `duration_minutes` | `int` | Длительность. |
| `completed_at` | `timestamptz` | Завершение. |

### `asset_inspections` — осмотры

| Поле | Тип | Назначение |
|---|---|---|
| `id`, `asset_id`, `inspector_user_id` | `uuid` | Осмотр, объект и проверяющий. |
| `kind`, `result`, `recommendation` | `text` | Вид, результат и рекомендация. |
| `defect_found` | `bool` | Обнаружен дефект. |
| `condition_score` | `double precision` | Оценка состояния. |
| `inspected_at` | `timestamptz` | Время осмотра. |

### `maintenance_plans` — планы обслуживания

| Поле | Тип | Назначение |
|---|---|---|
| `id`, `asset_id` | `uuid` | План и объект. |
| `kind` | `text` | Вид обслуживания. |
| `interval_days` | `int` | Период в днях. |
| `next_due_at` | `timestamptz` | Следующий срок. |
| `active` | `bool` | Активность. |
| `last_completed_at` | `timestamptz` | Последнее выполнение. |

Пара `(asset_id, kind)` уникальна.

### `failure_predictions` — оценки риска

| Поле | Тип | Назначение |
|---|---|---|
| `id`, `asset_id` | `uuid` | Прогноз и объект. |
| `risk_score` | `double precision` | Балл риска. |
| `risk_level` | `text` | Уровень. |
| `failure_probability_90d` | `double precision` | Расчетная вероятность на 90 дней. |
| `factors` | `jsonb` | Объясняющие причины. |
| `recommended_action` | `text` | Рекомендуемое действие. |
| `calculated_at` | `timestamptz` | Время расчета. |

### `outbox_events` — исходящие события

| Поле | Тип | Назначение |
|---|---|---|
| `id`, `aggregate_id` | `uuid` | Событие и объект. |
| `event_type` | `text` | Вид события. |
| `payload` | `jsonb` | Содержимое. |
| `status` | `text` | Состояние публикации. |
| `attempts` | `int` | Число попыток. |
| `next_attempt_at`, `locked_at`, `sent_at`, `created_at` | `timestamptz` | Планирование, захват, отправка и создание. |
| `last_error` | `text` | Последняя ошибка. |
